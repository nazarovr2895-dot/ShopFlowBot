# 🔄 Управление редиректами Nginx (301 vs 302)

## Проблема

**HTTP 301** (Permanent Redirect) - браузер кэширует **НАВСЕГДА**
**HTTP 302** (Temporary Redirect) - браузер **НЕ** кэширует

## Решение

Используем **разные конфигурации** для development и production.

---

## 📁 Файлы

```
nginx/
├── nginx.prod.conf      # Production (301 редиректы)
├── nginx.dev.conf       # Development (302 редиректы)
├── nginx.conf           # Симлинк → prod или dev
├── switch-config.sh     # Скрипт переключения
└── README.md            # Детальная документация
```

---

## 🚀 Быстрое использование

### Проверить текущую конфигурацию:

```bash
./nginx/switch-config.sh status
```

### Переключить на Development (302):

```bash
./nginx/switch-config.sh dev
docker compose restart nginx
```

### Переключить на Production (301):

```bash
./nginx/switch-config.sh prod
docker compose -f docker-compose.prod.yml restart nginx
```

---

## 💡 Когда что использовать

### Development (302) - Локально и для тестирования

```bash
./nginx/switch-config.sh dev
```

**Используйте когда:**
- ✅ Разрабатываете локально
- ✅ Тестируете новые редиректы
- ✅ Экспериментируете с доменами
- ✅ На staging сервере

**Преимущества:**
- Браузер НЕ кэширует редиректы
- Можно безопасно менять конфигурацию
- Легко тестировать изменения

### Production (301) - На production сервере

```bash
./nginx/switch-config.sh prod
```

**Используйте когда:**
- ✅ Production сервер
- ✅ Уверены что редиректы правильные
- ✅ Конфигурация проверена и стабильна

**Преимущества:**
- Быстрее (браузер не проверяет каждый раз)
- SEO оптимизация (поисковики понимают что редирект постоянный)

---

## 🔧 На сервере

### При первом деплое:

```bash
# На сервере
ssh yandex-cloud
cd ~/shopflowbot/nginx

# Убедиться что используется prod конфигурация
ls -la nginx.conf  # Должно быть: nginx.conf -> nginx.prod.conf

# Если нет - переключить
ln -sf nginx.prod.conf nginx.conf

# Перезапустить nginx
docker compose -f docker-compose.prod.yml restart nginx
```

### Проверка:

```bash
# Проверить какие редиректы используются
docker compose exec nginx cat /etc/nginx/conf.d/default.conf | grep "return 30"

# Production должен показать:
# return 301 https://...

# Development должен показать:
# return 302 https://...
```

---

## 🐛 Если браузер кэшировал старый 301 редирект

### Способ 1: Очистить HSTS (рекомендуется)

```
1. Открыть: chrome://net-internals/#hsts
2. В разделе "Delete domain security policies"
3. Ввести: app.flowshow.ru
4. Нажать: Delete
5. Перезапустить Chrome
```

### Способ 2: Hard Refresh

- **Mac**: `Cmd + Shift + R`
- **Windows**: `Ctrl + Shift + R`

### Способ 3: Режим инкогнито

Открыть сайт в режиме инкогнито - должно работать сразу.

---

## 📋 Чеклист перед production деплоем

- [ ] На локальной машине используется `nginx.dev.conf`
- [ ] На сервере используется `nginx.prod.conf`
- [ ] Проверили симлинк: `ls -la nginx/nginx.conf`
- [ ] Протестировали редиректы в браузере
- [ ] Перезапустили nginx после изменений

---

## 🎯 Workflow

### Разработка новой функции:

```bash
# 1. Локально - используем dev конфигурацию
./nginx/switch-config.sh dev
docker compose restart nginx

# 2. Тестируем изменения
# Браузер НЕ кэширует редиректы - можно безопасно экспериментировать

# 3. Коммитим изменения
git add nginx/
git commit -m "feat: обновлена nginx конфигурация"

# 4. Деплоим на сервер
./deploy.sh "feat: обновлена nginx конфигурация"

# 5. На сервере автоматически используется prod конфигурация (301)
```

---

## 📚 Детальная документация

См. `nginx/README.md` для подробной информации.

---

**Создано:** 2025-02-14
**Версия:** 1.0.0
