# 🤖 Автоматическое обновление документации

> Руководство по автоматизации обновления MD файлов проекта

---

## 🎯 Способы автоматизации

### Способ 1: Скрипт update-docs.sh (Простой)

Автоматически обновляет даты и коммитит изменения.

```bash
# Запустить обновление
./update-docs.sh
```

**Что делает:**
- Обновляет даты в MD файлах
- Показывает изменения
- Спрашивает подтверждение на коммит
- Опционально пушит в GitHub

---

### Способ 2: Git Hook (Автоматический при коммите)

Автоматически обновляет документацию при каждом коммите.

#### Настройка:

```bash
# Создать pre-commit hook
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/bash
# Автоматическое обновление дат в документации

CURRENT_DATE=$(date +%Y-%m-%d)

# Обновить даты в CLAUDE.md
if [ -f "CLAUDE.md" ]; then
    sed -i '' "s/Последнее обновление: .*/Последнее обновление: $CURRENT_DATE/" CLAUDE.md
    git add CLAUDE.md
fi

# Обновить даты в README.md
if [ -f "README.md" ]; then
    sed -i '' "s/Последнее обновление: .*/Последнее обновление: $CURRENT_DATE/" README.md
    git add README.md
fi

exit 0
EOF

# Сделать исполняемым
chmod +x .git/hooks/pre-commit
```

**Теперь при каждом коммите даты обновляются автоматически!**

---

### Способ 3: Claude Code (Умный помощник) ⭐ Рекомендуется

Просто попросите Claude обновить документацию:

```
Пример запросов к Claude:

1. "Обнови CLAUDE.md, добавь информацию о новом API endpoint /users"
2. "Обнови README.md, измени версию на 2.0.0"
3. "Добавь в CHEATSHEET.md команды для работы с Redis"
4. "Обнови всю документацию - изменилась структура проекта"
```

**Claude умеет:**
- ✅ Читать существующие MD файлы
- ✅ Понимать контекст проекта
- ✅ Обновлять нужные разделы
- ✅ Сохранять форматирование
- ✅ Коммитить изменения

**Преимущества:**
- Умное обновление (не просто дата, а реальный контент)
- Понимает структуру документа
- Может создавать новые разделы
- Автоматически форматирует Markdown

---

### Способ 4: Watch-скрипт (Отслеживание изменений)

Автоматически обновляет документацию при изменении кода.

```bash
# Установить fswatch (один раз)
brew install fswatch

# Создать watch-скрипт
cat > watch-and-update-docs.sh << 'EOF'
#!/bin/bash
echo "👀 Отслеживаю изменения в коде..."

fswatch -o backend/ bot/ miniapp/ adminpanel/ | while read change
do
    echo "🔄 Обнаружены изменения, обновляю документацию..."

    # Здесь можно добавить логику обновления
    # Например, обновить дату
    current_date=$(date +%Y-%m-%d)
    sed -i '' "s/Последнее обновление: .*/Последнее обновление: $current_date/" CLAUDE.md

    echo "✅ Документация обновлена: $current_date"
done
EOF

chmod +x watch-and-update-docs.sh
```

---

## 🛠️ Практические сценарии

### Сценарий 1: Добавили новый API endpoint

**Вручную:**
```bash
# Отредактировать CLAUDE.md
vim CLAUDE.md
# Добавить раздел про новый endpoint...
git add CLAUDE.md
git commit -m "docs: добавлен новый endpoint /users"
git push
```

**С Claude Code:**
```
Просто скажите Claude:
"Добавь в CLAUDE.md информацию о новом endpoint GET /users
который возвращает список всех пользователей"

Claude сам:
1. Прочитает CLAUDE.md
2. Найдет нужный раздел
3. Добавит информацию
4. Отформатирует
5. Сохранит
```

---

### Сценарий 2: Изменилась структура проекта

**С update-docs.sh:**
```bash
# Вручную отредактировать структуру в CLAUDE.md
vim CLAUDE.md

# Запустить скрипт обновления
./update-docs.sh
# Скрипт обновит даты и предложит закоммитить
```

**С Claude Code:**
```
"Обнови раздел 'Структура проекта' в CLAUDE.md -
добавилась папка backend/workers/"

Claude сам найдет раздел и обновит структуру.
```

---

### Сценарий 3: Новая команда в шпаргалке

**С Claude Code:**
```
"Добавь в CHEATSHEET.md команду для очистки Redis:
docker compose exec redis redis-cli FLUSHALL"

Claude добавит в нужный раздел с правильным форматированием.
```

---

## 🎨 Автоматизация создания новой документации

### При добавлении нового сервиса:

```bash
# Создать шаблон скрипт
cat > create-service-docs.sh << 'EOF'
#!/bin/bash
SERVICE_NAME=$1

if [ -z "$SERVICE_NAME" ]; then
    echo "Usage: ./create-service-docs.sh <service-name>"
    exit 1
fi

# Создать документацию для сервиса
cat > "${SERVICE_NAME}/README.md" << DOC
# ${SERVICE_NAME^} Service

## Описание

## Установка

## Использование

## API

## Тестирование

---
**Создано:** $(date +%Y-%m-%d)
DOC

echo "✅ Создана документация: ${SERVICE_NAME}/README.md"
EOF

chmod +x create-service-docs.sh
```

**Использование:**
```bash
./create-service-docs.sh payments
# Создаст payments/README.md с шаблоном
```

---

## 📋 Чеклист обновления документации

### При добавлении функциональности:

- [ ] Обновить CLAUDE.md (главный гайд)
- [ ] Добавить в CHEATSHEET.md (если есть новые команды)
- [ ] Обновить README.md (если изменился overview)
- [ ] Добавить примеры в соответствующий раздел
- [ ] Обновить дату "Последнее обновление"
- [ ] Закоммитить: `git commit -m "docs: ..."`

### При изменении архитектуры:

- [ ] Обновить диаграмму в CLAUDE.md
- [ ] Обновить раздел "Структура проекта"
- [ ] Обновить docker-compose примеры
- [ ] Обновить переменные окружения в .env.example

---

## 🤖 Лучшие практики

### 1. Используйте Claude Code для умных обновлений

**Хорошо:**
```
"Обнови CLAUDE.md - добавь информацию о rate limiting"
```

Claude поймет контекст и добавит в нужное место.

**Плохо:**
```bash
# Вручную искать нужное место
vim CLAUDE.md
# Искать раздел... скроллить... редактировать...
```

---

### 2. Обновляйте документацию сразу

**Хорошо:**
```bash
# Добавили функцию
git add backend/app/api/users.py

# Сразу обновили документацию
# Через Claude или вручную
git add CLAUDE.md

# Один коммит
git commit -m "feat: добавлен users API endpoint"
```

**Плохо:**
```bash
# Добавили функцию
git commit -m "feat: users API"

# Через неделю вспомнили про документацию
git commit -m "docs: забыл обновить документацию"
```

---

### 3. Автоматизируйте рутину

**Даты** - автоматически (git hook)
**Структурные изменения** - через Claude Code
**Новые команды** - через update-docs.sh

---

## 🔄 Workflow обновления

### Ежедневная работа:

```bash
# 1. Внесли изменения в код
vim backend/app/api/sellers.py

# 2. Обновили документацию через Claude Code
# Просто попросите: "Обнови CLAUDE.md про sellers API"

# 3. Коммит (даты обновятся автоматически через git hook)
git add .
git commit -m "feat: улучшен sellers API"

# 4. Деплой
./deploy.sh "feat: улучшен sellers API"
```

---

## 💡 Советы

### 1. Держите CLAUDE.md актуальным

Это главный файл - обновляйте его первым.

### 2. Используйте шаблоны

Создайте шаблоны для частых секций:

```bash
# Шаблон для нового API endpoint
cat > templates/api-endpoint.md << 'EOF'
### X. Название endpoint

**Файлы:** `backend/app/api/...`

```python
@router.get("/endpoint")
async def endpoint_name(...):
    # Описание
    return {...}
```

Зарегистрировать в `main.py`:
```python
app.include_router(...)
```
EOF
```

### 3. Версионируйте документацию

```bash
# При major релизе копируйте документацию
cp CLAUDE.md docs/archive/CLAUDE-v1.0.0.md
```

---

## ⚙️ Настройка IDE для быстрого доступа

### VS Code Snippets:

```json
// .vscode/markdown.code-snippets
{
  "Update Date": {
    "prefix": "update-date",
    "body": [
      "**Последнее обновление:** ${CURRENT_YEAR}-${CURRENT_MONTH}-${CURRENT_DATE}"
    ]
  }
}
```

---

## 🎯 Итого: Рекомендуемый подход

**Для простых обновлений (даты, мелкие правки):**
```bash
./update-docs.sh
```

**Для умных обновлений (новые разделы, сложные изменения):**
```
Попросите Claude Code:
"Обнови CLAUDE.md - добавь информацию о..."
```

**Для автоматизации рутины:**
```bash
# Настроить git hook (один раз)
# Даты будут обновляться автоматически при каждом коммите
```

**Для массовых изменений:**
```bash
# Используйте find + sed
find . -name "*.md" -exec sed -i '' "s/старое/новое/g" {} \;
```

---

## 📞 Помощь

Если нужно обновить документацию, но не знаете как:

1. **Простые изменения** → `./update-docs.sh`
2. **Сложные изменения** → попросите Claude Code
3. **Автоматизация** → настройте git hook
4. **Создание новой документации** → используйте шаблоны

---

**Обновлено:** 2025-02-14
