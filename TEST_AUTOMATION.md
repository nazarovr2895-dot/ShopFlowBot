# Test Automation — Автоматизация тестирования

> Полная автоматизация запуска тестов на всех этапах разработки

---

## 🎯 Что автоматизировано

### ✅ 1. Pre-commit Hook (перед коммитом)
Тесты запускаются автоматически **перед каждым `git commit`**

### ✅ 2. Deploy Script (при деплое)
Тесты запускаются автоматически **перед деплоем на сервер**

### ✅ 3. GitHub Actions (при push)
Тесты запускаются автоматически **на GitHub при каждом push**

---

## 📋 Установка

### Шаг 1: Pre-commit Hook (локально)

```bash
# Скопировать hook в .git/hooks/
cp scripts/pre-commit-hook.sh .git/hooks/pre-commit

# Сделать исполняемым
chmod +x .git/hooks/pre-commit

# Готово! Теперь тесты будут запускаться перед каждым коммитом
```

**Что делает:**
- Автоматически запускается при `git commit`
- Проверяет, есть ли изменения в `backend/`
- Если есть — запускает тесты
- Если тесты падают — коммит отменяется
- Если тесты проходят — коммит создаётся

**Как отключить для конкретного коммита:**
```bash
# Пропустить pre-commit hook (НЕ РЕКОМЕНДУЕТСЯ!)
git commit --no-verify -m "WIP: без тестов"
```

---

### Шаг 2: Deploy Script (уже настроен)

Deploy script (`deploy.sh`) уже обновлён и автоматически запускает тесты.

**Что делает:**
- При запуске `./deploy.sh "message"` проверяет изменения в `backend/`
- Если есть изменения — запускает тесты
- Если тесты падают — **деплой отменяется**
- Если тесты проходят — продолжает деплой на сервер

**Использование:**
```bash
# Обычный деплой (с тестами)
./deploy.sh "feat: add new feature"

# Если изменений в backend нет — тесты пропускаются
./deploy.sh "docs: update README"
```

---

### Шаг 3: GitHub Actions (уже настроен)

GitHub Actions настроен в `.github/workflows/tests.yml`

**Что делает:**
- Запускается автоматически при `git push` в ветки `main`, `master`, `develop`
- Запускается только если есть изменения в `backend/` или `.github/workflows/tests.yml`
- Устанавливает Python 3.12
- Устанавливает зависимости из `requirements.txt`
- Запускает тесты
- Генерирует отчёт о покрытии кода
- Показывает результат в интерфейсе GitHub (зелёная галочка или красный крестик)

**Где смотреть результаты:**
1. Зайдите на GitHub в ваш репозиторий
2. Перейдите во вкладку **Actions**
3. Выберите последний запуск **Backend Tests**
4. Посмотрите логи и результат тестов

**Статус тесты в PR:**
При создании Pull Request GitHub автоматически покажет статус тестов:
- ✅ Зелёная галочка — тесты прошли, можно мержить
- ❌ Красный крестик — тесты провалились, нужно исправить

---

## 🔄 Workflow разработки

### Обычная разработка (с pre-commit hook)

```bash
# 1. Создаёте новую функцию и тесты
vim backend/app/api/my_feature.py
vim backend/tests/test_my_feature.py

# 2. Запускаете тесты вручную
source backend/venv/bin/activate
pytest backend/tests/test_my_feature.py -v

# 3. Коммитите (тесты запустятся автоматически!)
git add .
git commit -m "feat: add my feature"
# → Pre-commit hook запустит тесты
# → Если тесты падают — коммит отменится
# → Если тесты проходят — коммит создастся

# 4. Пушите на GitHub
git push
# → GitHub Actions запустит тесты
# → Результат будет виден во вкладке Actions

# 5. Деплой на сервер
./deploy.sh "feat: add my feature"
# → Тесты запустятся ещё раз перед деплоем
# → Если тесты падают — деплой отменится
# → Если тесты проходят — деплой продолжится
```

### Быстрая разработка (без автоматических тестов)

Если нужно быстро закоммитить WIP без тестов:

```bash
# Пропустить pre-commit hook
git commit --no-verify -m "WIP: in progress"

# НО ЛУЧШЕ использовать git stash
git stash
git checkout -b feature/my-feature
git stash pop
# Работаете в отдельной ветке, тесты не мешают
```

---

## 🧪 Какие тесты запускаются

### При pre-commit и deploy:

Запускаются **только новые критичные тесты** (быстро, ~10-15 секунд):

```bash
pytest backend/tests/test_admin.py \
       backend/tests/test_buyers.py \
       backend/tests/test_seller_web.py \
       backend/tests/test_services.py
```

**Итого: 150 тестов**
- 33 теста admin API
- 31 тест buyers API
- 33 теста seller_web API
- 53 теста services (unit-тесты)

### При GitHub Actions:

Запускаются **те же тесты + генерируется отчёт о покрытии**

---

## 📊 Мониторинг качества кода

### GitHub Status Checks

После push на GitHub вы увидите статус тестов:

```
✅ Backend Tests / test (3.12) — Passed
```

Или:

```
❌ Backend Tests / test (3.12) — Failed
```

### Coverage Report

GitHub Actions генерирует отчёт о покрытии кода тестами.

**Как посмотреть:**
1. Зайдите в GitHub Actions
2. Откройте последний запуск **Backend Tests**
3. Раскройте шаг **Run tests with coverage**
4. Посмотрите отчёт в логах

---

## 🛠️ Настройка и кастомизация

### Изменить список запускаемых тестов

**Pre-commit hook** (`scripts/pre-commit-hook.sh`):

```bash
# Найдите строку:
pytest backend/tests/test_admin.py \
       backend/tests/test_buyers.py \
       backend/tests/test_seller_web.py \
       backend/tests/test_services.py \
       -q --tb=short

# Добавьте свои тесты:
pytest backend/tests/test_admin.py \
       backend/tests/test_buyers.py \
       backend/tests/test_seller_web.py \
       backend/tests/test_services.py \
       backend/tests/test_my_new_feature.py \
       -q --tb=short
```

**Deploy script** (`deploy.sh`):

```bash
# Найдите аналогичную строку и добавьте свои тесты
```

**GitHub Actions** (`.github/workflows/tests.yml`):

```yaml
# Найдите секцию:
- name: 🧪 Run new tests
  run: |
    pytest backend/tests/test_admin.py \
           backend/tests/test_buyers.py \
           backend/tests/test_seller_web.py \
           backend/tests/test_services.py \
           backend/tests/test_my_new_feature.py \
           -v --tb=short
```

### Отключить автоматические тесты

**Pre-commit hook:**
```bash
# Удалите файл
rm .git/hooks/pre-commit
```

**Deploy script:**
```bash
# Закомментируйте секцию с тестами в deploy.sh
# или используйте флаг --skip-tests (можно добавить)
```

**GitHub Actions:**
```bash
# Удалите или переименуйте файл
mv .github/workflows/tests.yml .github/workflows/tests.yml.disabled
```

---

## 🚨 Что делать, если тесты падают

### При pre-commit

```bash
# Тесты упали при коммите
❌ Tests failed! Fix them before committing.

# Вариант 1: Исправить тесты
vim backend/tests/test_my_feature.py
source backend/venv/bin/activate
pytest backend/tests/test_my_feature.py -v
git add .
git commit -m "fix: update tests"

# Вариант 2: Пропустить hook (НЕ РЕКОМЕНДУЕТСЯ)
git commit --no-verify -m "WIP: will fix tests later"
```

### При деплое

```bash
# Тесты упали при деплое
❌ Тесты провалились! Деплой отменён.

# Исправьте тесты ПЕРЕД деплоем
source backend/venv/bin/activate
pytest backend/tests/ -v

# После исправления попробуйте снова
./deploy.sh "fix: tests fixed"
```

### При GitHub Actions

```bash
# Тесты упали на GitHub
# 1. Посмотрите логи в Actions
# 2. Исправьте локально
# 3. Закоммитьте и запушьте снова

git add .
git commit -m "fix: resolve failing tests"
git push
```

---

## 📈 Метрики качества

### Целевые показатели

- ✅ **100%** новых эндпоинтов покрыты тестами
- ✅ **80%+** общее покрытие кода (coverage)
- ✅ **0** падающих тестов в main ветке
- ✅ **<30 секунд** время выполнения критичных тестов

### Текущие показатели

- **150 новых тестов** написано
- **226 тестов** всего в backend
- **100% SUCCESS** на новых тестах
- **~15 секунд** время выполнения критичных тестов

---

## 📚 Дополнительные ресурсы

- [TDD_GUIDE.md](./TDD_GUIDE.md) — как писать тесты для новых функций
- [TESTS_GUIDE.md](./TESTS_GUIDE.md) — полное руководство по тестированию
- [RUN_TESTS.md](./RUN_TESTS.md) — как запускать тесты на сервере
- [scripts/pre-commit-hook.sh](./scripts/pre-commit-hook.sh) — pre-commit hook
- [.github/workflows/tests.yml](./.github/workflows/tests.yml) — GitHub Actions

---

## ✅ Контрольный список

После установки автоматизации проверьте:

- [ ] ✅ Pre-commit hook установлен (`ls -la .git/hooks/pre-commit`)
- [ ] ✅ Pre-commit hook исполняемый (`chmod +x .git/hooks/pre-commit`)
- [ ] ✅ Deploy.sh обновлён (содержит секцию с тестами)
- [ ] ✅ GitHub Actions настроен (файл `.github/workflows/tests.yml` существует)
- [ ] ✅ Виртуальное окружение создано (`backend/venv/`)
- [ ] ✅ Зависимости установлены (`source backend/venv/bin/activate && pip list`)
- [ ] ✅ Тесты запускаются вручную (`pytest backend/tests/test_services.py -v`)
- [ ] ✅ Тесты запускаются при коммите (попробуйте `git commit`)
- [ ] ✅ Тесты запускаются при деплое (попробуйте `./deploy.sh "test"`)

---

**Автоматизация настроена! Теперь тесты защищают ваш код от регрессий. 🛡️**

*Последнее обновление: 2025-02-15*
